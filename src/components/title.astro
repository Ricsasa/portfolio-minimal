---
const { title } = Astro.props;
---

<div class="w-full mb-12 md:mb-24">
    {
        title && (
            <h1 class="font-sans text-5xl font-bold tracking-[-0.03em] md:text-6xl border-b-2 border-pine pb-6 inline-block">
                {title}
            </h1>
        )
    }
</div>
